{
  "name": "@kivv/automation",
  "version": "1.0.0",
  "description": "Automation Worker for kivv - Daily arXiv paper collection and AI summarization",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0",
    "fast-xml-parser": "^4.5.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.26",
    "@cloudflare/workers-types": "^4.20241127.0",
    "miniflare": "^3.20241127.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^3.102.0"
  }
}
